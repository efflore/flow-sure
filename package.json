{
  "name": "result",
  "version": "0.9.0",
  "description": "A Result monad in TypeScript. Use option() to handle maybe nullish values, result() to handle possible errors, and task() for asynchronous tasks with retry. The data types Ok, Nil, and Err all implement methods like map(), flatMap(), filter(), guard(), or(), catch(), match(), and get().",
  "main": "index.js",
  "scripts": {
	"build": "bun build index.ts --outdir ./ --minify",
	"test": "echo \"Error: no test specified\" && exit 1"
  },
  "module": "index.ts",
  "type": "module",
  "author": "Esther Brunner",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}