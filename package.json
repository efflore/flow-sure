{
  "name": "@efflore/flow-sure",
  "version": "0.9.4",
  "description": "FlowSure - a Result monad in TypeScript. Data types Ok, Nil, Err with ensure(), attempt() async gather() and flow() functions.",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
	"build": "bun build index.ts --outdir ./ --minify && bunx tsc",
	"test": "echo \"Error: no test specified\" && exit 1"
  },
  "module": "index.ts",
  "type": "module",
  "author": "Esther Brunner",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}